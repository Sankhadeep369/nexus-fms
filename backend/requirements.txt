fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.2.1
pydantic-settings==2.7.1
diskcache==5.6.3
huggingface_hub==0.27.1
python-multipart==0.0.20

# Retrieval: BM25 (lexical) + sentence-transformers (dense) hybrid search over
# 02_extracted_text/
rank-bm25==0.2.2
sentence-transformers==3.3.1

# Groq SDK — used by the output validator (LLM judge) to verify generated answers
# before returning them to the user. Fast inference via Groq's free API tier.
groq==0.13.1

# Reminder Agent: Supabase (persistent reminder storage) and Resend (email delivery)
supabase==2.31.0
resend==2.32.2
email-validator==2.3.0

# Self-serve document ingestion: PDF/DOCX text extraction + OCR (Tesseract via
# pdf2image/poppler) for scanned PDFs. Needs the tesseract-ocr + poppler-utils
# system packages (installed in the Dockerfile).
pypdf==5.1.0
python-docx==1.1.2
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.0.0

# llama-cpp-python is installed separately from a prebuilt CPU wheel index because
# building it from source requires a C++ toolchain (MSVC/nmake) that isn't installed:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
