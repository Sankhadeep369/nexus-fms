fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.2.1
pydantic-settings==2.7.1
diskcache==5.6.3
huggingface_hub==0.27.1
python-multipart==0.0.20

# Retrieval: BM25 (lexical) + sentence-transformers (dense) hybrid search over
# 02_extracted_text/
rank-bm25==0.2.2
sentence-transformers==3.3.1

# llama-cpp-python is installed separately from a prebuilt CPU wheel index because
# building it from source requires a C++ toolchain (MSVC/nmake) that isn't installed:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
